# Test requirements for conversation replayer
# These dependencies are needed for running the test suite

# Core dependencies (should already be installed)
pydantic>=2.0.0
anthropic>=0.40.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0

# Optional for enhanced testing
freezegun>=1.2.0  # For mocking datetime in tests
